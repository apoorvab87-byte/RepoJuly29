/* import { test } from '@playwright/test';

test('Print fruits containing apple', async ({ page }) => {

    const fruits = ["apple", "pineapple", "greenapple", "mango", "orange"];

    const result = fruits.filter(fruit => fruit.includes("apple"));

    console.log("Fruits containing apple:", result);
});
 */


/* import { test } from '@playwright/test';

test('Print fruits containing apple', async ({ page }) => {


const fruits = ["apple", "pineapple", "greenapple", "mango", "orange"];

const result = fruits.filter(fruit =>fruit.includes("app"));

 console.log("Fruits containing apple:", result);
}); */